package apple.npc.creation.convo.info;

/**
 * a local category for a conversation (used for creation of a local category in a file)
 */
public class ConvoLocalInfo {
    public int uid;
    public String name;


    public ConvoLocalInfo(int uid, String name) {
        this.uid = uid;
        this.name = name;
    }
}
