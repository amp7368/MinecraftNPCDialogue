package apple.npc.reading.command;

import apple.npc.reading.Reading;
import org.bukkit.entity.Player;

public class ReadingBoolean implements Reading {
    public ReadingBoolean(String global, int local, int convo, int response, ResponseType type, String newGlobal, int newLocalUID, int newLocalUID1, Player player) {
    }

    @Override
    public void dealWithStop(Player player) {

    }
}
